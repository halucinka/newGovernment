'use strict';

Object.defineProperty(exports, '__esModule', {
    value: true
});
exports.IsEmail = IsEmail;
exports.IsRequired = IsRequired;
exports.HasNumber = HasNumber;
exports.HasLength = HasLength;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _bluebird = require('bluebird');

var _bluebird2 = _interopRequireDefault(_bluebird);

var _validator = require('validator');

var _validator2 = _interopRequireDefault(_validator);

function IsEmail(value, _ref) {
    var msg = _ref.msg;

    return _bluebird2['default'].resolve(_validator2['default'].isEmail(value) || msg || 'Please enter a valid email.');
}

function IsRequired(value, _ref2) {
    var msg = _ref2.msg;

    return _bluebird2['default'].resolve(value != null && value.length > 0 || msg || 'This field is required.');
}

function HasNumber(value, _ref3) {
    var msg = _ref3.msg;

    return _bluebird2['default'].resolve(value == null || value.match(/.*[0-9]+.*/i) === null ? msg || 'This field should contain at least one number.' : true);
}

function HasLength(value, _ref4) {
    var msg = _ref4.msg;
    var min = _ref4.min;
    var max = _ref4.max;

    return _bluebird2['default'].resolve(!value || (min == null || value.length >= min) && (max == null || value.length <= max) || msg || 'Length should be between ' + min + ' and ' + max);
}